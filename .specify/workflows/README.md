# 场景选择决策树

> **Version**: 1.0.0  
> **Created**: 2026-01-02

---

## 快速决策

使用以下决策树选择合适的场景：

```
开始
  │
  ▼
┌─────────────────────────────┐
│  specs/{feature}/ 目录存在？  │
└─────────────────────────────┘
         │
    ┌────┴────┐
    │         │
   否        是
    │         │
    ▼         ▼
┌───────┐  ┌─────────────────────────┐
│       │  │     变更规模多大？       │
│ 场景一 │  └─────────────────────────┘
│       │           │
│ 新功能 │     ┌─────┼─────┬──────┐
└───────┘     │     │     │      │
              │     │     │      │
             小    中    大    重构
              │     │     │      │
              ▼     ▼     ▼      ▼
          ┌─────┐┌─────┐┌─────┐┌─────┐
          │场景二││场景三││场景三││场景四│
          │小改动││ 扩展 ││ 扩展 ││ 重构 │
          └─────┘└─────┘└─────┘└─────┘
```

---

## 场景判断标准

| 场景 | 文档存在 | 变更规模 | 新接口 | 架构变更 | 破坏性 |
|------|----------|----------|--------|----------|--------|
| **场景一** | ❌ 不存在 | 全新 | ✅ 是 | ✅ 是 | - |
| **场景二** | ✅ 存在 | < 50 行 | ❌ 否 | ❌ 否 | ❌ 否 |
| **场景三** | ✅ 存在 | 中等 | ✅ 可能 | ❌ 否 | ❌ 否 |
| **场景四** | ✅ 存在 | 大 | ✅ 是 | ✅ 是 | ✅ 是 |

---

## 场景一：新项目新功能 🆕

**适用条件**：
- 全新项目的首个功能
- 现有项目中的全新功能模块
- 不存在任何相关的 spec/plan/tasks 文档

**工作流**：
```
Constitution → Specify → Plan → Tasks → Implement
     ↓           ↓        ↓       ↓        ↓
   (首次)      spec.md  plan.md tasks.md  代码
```

**触发命令**：
- Cursor: `/speckit-scenario-new`
- Claude Code: `/speckit.scenario.new`

**详细指南**：[scenario-1-new.md](./scenario-1-new.md)

---

## 场景二：已有功能小改动 🔧

**适用条件**：
- Bug 修复
- 参数调整
- 性能优化
- 代码量 < 50 行
- 不涉及新接口或数据库变更

**工作流**：
```
识别改动点 → 更新 tasks.md → 执行实现 → 更新版本历史
```

**触发命令**：
- Cursor: `/speckit-scenario-update`
- Claude Code: `/speckit.scenario.update`

**详细指南**：[scenario-2-update.md](./scenario-2-update.md)

---

## 场景三：已有功能扩展 ➕

**适用条件**：
- 添加子功能
- 扩展 API 接口
- 添加数据字段
- 保持现有架构
- 向后兼容

**工作流**：
```
分析扩展点 → 扩展 spec.md → 扩展 plan.md → 更新 tasks.md → 实现
                 ↓              ↓              ↓
            (添加 Story)   (添加接口)     (添加任务)
```

**触发命令**：
- Cursor: `/speckit-scenario-extend`
- Claude Code: `/speckit.scenario.extend`

**详细指南**：[scenario-3-extend.md](./scenario-3-extend.md)

---

## 场景四：已有功能重构 🔄

**适用条件**：
- 架构重构
- 接口签名变更
- 数据库结构重大调整
- 涉及破坏性变更
- 需要数据迁移

**工作流**：
```
审查现有文档 → 创建 v2 目录 → 重新设计 → 迁移任务 → 执行迁移
                   ↓
              specs/{feature}/
              ├── v1/  (保留)
              └── v2/  (新版)
```

**触发命令**：
- Cursor: `/speckit-scenario-refactor`
- Claude Code: `/speckit.scenario.refactor`

**详细指南**：[scenario-4-refactor.md](./scenario-4-refactor.md)

---

## 常见问题

### Q: 如何选择场景二和场景三？

**场景二（小改动）**：
- 代码量 < 50 行
- 不涉及新接口
- 不涉及数据库变更
- 不需要更新 spec.md（除了版本历史）

**场景三（扩展）**：
- 涉及新的 User Story
- 需要新增 API 接口
- 需要添加数据库字段
- 需要更新多个文档

### Q: 如何选择场景三和场景四？

**场景三（扩展）**：
- 向后兼容
- 只是添加新功能
- 不修改现有接口
- 不删除现有功能

**场景四（重构）**：
- 涉及破坏性变更
- 需要修改现有接口
- 需要数据迁移
- 需要版本化管理

### Q: 可以中途切换场景吗？

可以。如果在执行过程中发现变更规模超出预期：
- 场景二 → 场景三：发现需要新接口
- 场景三 → 场景四：发现需要破坏性变更

切换时，需要从头开始执行新场景的流程。

---

## 工具命令速查

| 场景 | Cursor 命令 | Claude Code 命令 |
|------|-------------|------------------|
| 场景一：新功能 | `/speckit-scenario-new` | `/speckit.scenario.new` |
| 场景二：小改动 | `/speckit-scenario-update` | `/speckit.scenario.update` |
| 场景三：扩展 | `/speckit-scenario-extend` | `/speckit.scenario.extend` |
| 场景四：重构 | `/speckit-scenario-refactor` | `/speckit.scenario.refactor` |

---

## 参考文档

- [效益分析](../doc/scenarios/benefits-analysis.md)
- [Cursor 使用指南](../doc/cursor-speckit-guide.md)
- [Claude Code 使用指南](../doc/claude-code-guide.md)
